{
  "name": "distry",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "precommit": "lint-staged && ./scripts/precommit-checks.sh"
  },
  "lint-staged": {
    "*.go": "gofmt -w",
    "frontend/**/*.{js,jsx,ts,tsx,css,json,html,md}": "prettier --write"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4"
  }
}
